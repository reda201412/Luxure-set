export const metadata = {
  title: "Luxure Set",
  description: "Premium & Free adult content",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}